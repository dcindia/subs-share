<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            margin: 0px;
            background-color: antiquewhite;
        }
        button:hover {
            background-image: linear-gradient(rgb(0 0 0/10%) 0 0);
        }
        button > .icon {
            align-self: center;
        }
        #header {
            padding: 0 20px;
            background-color: whitesmoke;
            box-sizing: border-box;
            display: flex;
            flex-direction: row;
            justify-content: space-between;
            position: sticky;
            top: 0;
        }
        #user-header {
            align-self: center;
        }
        #content {
            background-color:lightsalmon;
            display: flex;
            flex-direction: column;
            justify-content: flex-start;
            padding: 3vw;
            margin-top: 50px;
            margin: 8%;
            border-radius: 15px;
            border-style: groove;
            border: 4px solid indianred;
            overflow: scroll;
            scrollbar-width: none;
        }
        #error-mssage {
            font-family: Arial, Helvetica, sans-serif
        }
        #error-message > p {
            font-family: Arial, Helvetica, sans-serif;
            font-weight: 400;
            padding-left: 8vw;
        }
        #action-button {
            display: flex;
            width: fit-content;
            font-family: Arial, Helvetica, sans-serif;
            font-weight: 600;
            border-radius: 6px;
            border-width: 0;
            margin-left: 8vw;
            padding: 0.7em 0.7em;
            background-color: #4cc366;
        }
        #footer {
            position:fixed;
            bottom: 0px;
            width: 100%;
            background-color: black;
            color: white;
            text-align: center;
            font-size: small;
            margin-top: 20px;
            padding: 20px;
        }

    </style>

</head>

<body>
    <section id="header">
        <a href="/"><img id="logo-header" src="/static/logo.svg" width="70px" height="70px"></a>
        <div id="user-header"><strong id="title-header">yt-subscribes</em>
        </div>
    </section>
    <section id="content">
        <div id="error-message">
            <h3>Error encountered:</h3>
            <p>{{error_message}}</p>
        </div>
        <button id="action-button">Signup/Login<div class="hide-on-small">&nbsp;with </div>&nbsp;<i class="fa-brands fa-google icon"></i></button>
    </section>
    <div id="footer">Copyright: Dynamic Challengers 2023</div>
</body>
</html>
    
